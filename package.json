{
  "name": "secret-sentinel-scanner-rotator",
  "version": "0.1.0",
  "description": "A small TypeScript CLI that scans files for secret-like patterns and supports pluggable rotators (dry-run/apply).",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
  "build": "tsc -p .",
  "start": "node dist/index.js",
  "dev": "ts-node src/index.ts",
  "test": "vitest run"
  },
  "keywords": ["secret", "scanner", "rotator", "typescript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.5.2",
    "vitest": "^1.0.0",
    "@types/node": "^20.3.1"
  },
  "dependencies": {
    "ignore": "^5.2.0"
  }
}
